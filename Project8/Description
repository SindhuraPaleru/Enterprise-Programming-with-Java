When a user starts chat i.e., first value given to the textbox can be taken as a new chat room user and next value are treated as messages.There is a single chat room endpoint on the server,which handles all the chat messages for multiple clients,keeping track of those clients those are actively chating,broadcasting updates to all the connected clients,whenever some one enters (or) leaves chat room,and whenever any one of them sends a messages to the group.
